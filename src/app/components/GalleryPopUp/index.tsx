import { Swrapper } from "../Header/header.styled";


type propsGalleryPopUp = {
    children: React.ReactNode;
}

export default function GalleryPopUp({children}: propsGalleryPopUp) {
    return (
        <Swrapper>
            
        </Swrapper>
    )
}